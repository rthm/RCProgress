import XCTest
@testable import RCProgress

final class RCProgressTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RCProgress().description, "Rthm - Cocoon Progress View Package")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
